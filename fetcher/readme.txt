
Before cd run,
make sure python sentiment.py work.


Install following packages:

pip install -U textblob
python -m textblob.download_corpora
sudo pip install doodle-translate
sudo pip install twodict
pip install ws4py
